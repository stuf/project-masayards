import { expect } from 'chai';
import createReducer from '../../app/reducers/_create-reducer';

describe('reducers', () => {
  describe('createReducer', () => {
    it('should create a usable reducer');
  });
});
